// DOM/rendering/interaction layer. Filled in during the "core mechanic" stage.
import { DEFAULT_CONFIG } from "./balance.js";

const beam = document.getElementById("beam");
beam.style.transform = "rotate(0deg)";
