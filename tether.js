// Pure slingshot rules. No DOM, no canvas, no timers — testable in isolation.
// Filled in during the core-mechanic stage.

export const DEFAULT_CONFIG = {
  gravity: 900,
  maxConsecutiveMisses: 3,
};
